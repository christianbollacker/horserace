Horse Race the game is currently a work in progess.  It was posted only for my personal web access.

The HTML is mostly in place.  The CSS is in need of work.  Currently the JS is in an HTML file inside a script tag to I can test it independently.  horsescript.html